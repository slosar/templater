[project]
name = "templater"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "fitsio>=1.3",
    "numpy>=1.24",
    "torch>=2.0",
    "matplotlib>=3.7",
    "notebook>=7.0",
    "jax[cuda12]>=0.4",
    "optax>=0.2",
]
